1.读取数据，划分数据集
2.因为LeNet只接收32*32*n尺寸，所以需要对mnist进行填充,这里对照片上下进行填充，也就是shape[2:3]
3.打乱样本
4.建立LeNet网络
    a.第一、二层，卷积核是5*5*6，最大池是2*2，步长为2
    b.第三、四层，卷积核是5*5*16，最大池是2*2，步长为2
    c.第五层，输入：5*5*15（上一层输出），输出权重数量120
    d.第六层，权重数量84
    e.第七层，权重数量10
5.创建x,y的占位符，y的one hot
6.计算cnn最后一层的softmax以及cross entropy，用softmax_cross_entropy_with_logits
7.用reduce_mean计算向量平均值
8.使用AdamOptimizer自适应优化算法优化最小值（这里应该也可以用SGD）
9.计算准确度和精确度